{
  "name": "auto-chatter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cross-env DEV=1 tsx watch src/server.ts",
    "build": "tsc && vite build",
    "build:client": "vite build",
    "start": "node dist/server.js",
    "pretest": "tsc && vite build",
    "test": "node --test && npx playwright test",
    "test:unit": "node --test",
    "test:e2e": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/express": "^5.0.6",
    "@types/node": "^25.4.0",
    "agent-browser": "^0.17.1",
    "cross-env": "^10.1.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@lit/task": "^1.0.3",
    "express": "^5.2.1",
    "lit": "^3.3.2",
    "navigo": "^8.11.1"
  }
}
